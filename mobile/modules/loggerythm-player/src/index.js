'use strict';

// The app-facing TypeScript adapter owns the public API. Keeping this package's
// JavaScript surface empty prevents native implementation details becoming a
// second, accidental playback contract.
module.exports = {};
